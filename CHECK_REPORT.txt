《my-game 專案檢查報告》

版本：V173.65
更新日期：2026-09-14
基線分支：dev
更新起點：e0e8fe0fe4c65a6262f6e88674f5c058adeeb799
Game / Cache Version：V173.65 / 173.65

本文件是目前專案測試與資產數字的快速判讀基線。
V173.4 與更早版本的數字已失效；需要歷史資料時請查 Git history，不得再把舊報告中的數字當成目前專案現況。

一、目前 V173.65 測試基線

最近一次已建立並完整驗證的 V173.65 基線：
- JavaScript syntax：228 / 228 通過。
- Node / browser suites：152 / 152 通過。
- release / loader / git-diff gates：通過。
- 相關手機尺寸 Chrome QA、boot／skill／relic QA 與部署後 live QA：通過（完整驗證基線建立於 2026-09-11 的 dev 整合）。

目前 dev HEAD（e0e8fe0fe4c65a6262f6e88674f5c058adeeb799）：
- GitHub Actions CI：SUCCESS。
- CI 已有 diff-scope / guard 優化；若某次 HEAD 只修改不影響特定 suite 的檔案，該 suite 可以被 skip。
- 因此「最新一次 CI 成功」不等於「該次 run 重新執行全部 152 suites」。需要宣稱全量測試時，必須確認該 run 實際執行範圍，或執行完整測試基線。

二、目前 V173.65 資產／build 基線

最近一次完整 V173.65 deterministic build 驗證基線：
- resources：299。
- static IDs：242。
- deterministic build assets：22。
- deterministic build、release 與 loader gate：通過。

上述數字是目前用來判斷資產／build 是否異常的權威基線；不得再使用 V173.4 的 assets、靜態引用、Loader 或舊檔案數量做比較。

三、已淘汰的 V173.4 數字

以下 V173.4 舊值只代表 2026-08-31 當時狀態，已全部淘汰，不得用於目前專案判讀：
- Git 追蹤檔案 351。
- JavaScript syntax 150 / 150。
- Node suites 37 / 37。
- assets/ 142 個檔案。
- 靜態本地引用 300 個。
- 以及舊報告中所有 V173.4 HTML／CSS／JS／Loader／圖片格式與檔案數量。

四、後續 AI／維護者判讀規則

1. 判斷目前專案狀態時，先以最新 dev HEAD、最新 CI 與本文件 V173.65 基線為準。
2. 不得因歷史 test 檔、舊 HANDOFF 段落或 Git history 中的 V173.4 數字，回退目前基線。
3. CI 若因 diff-scope 顯示 skipped，必須區分「本次未受影響而略過」與「測試失敗／不存在」。
4. 若新增／刪除正式 script、test suite、resource、static ID 或 deterministic build asset，使基線數字永久改變，必須在同一輪更新本文件。
5. Game / Cache Version 若仍為 V173.65，但結構基線已改變，也必須更新本文件的日期與新數字，不能只看版本號判斷。
6. 若本文件與最新 dev 的實際 CI／build 輸出衝突，以最新 dev 的可重現檢查結果為準，並立即修正本文件。

結論：
- 目前發布版本基線為 V173.65。
- 已知完整測試基線：228 / 228 JavaScript syntax、152 / 152 Node / browser suites。
- 已知資產／build 基線：299 resources、242 static IDs、22 deterministic build assets。
- V173.4 與更早的數字僅屬歷史紀錄，不再代表目前專案。
