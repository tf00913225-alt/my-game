# 火之冒險者：永久 UI 開發規範

本文件是本遊戲專案所有 UI 開發工作的唯一規範來源。後續任何 UI 新增、
修改、重構或美術套用都必須先閱讀並遵守本文件，不得只依賴聊天記憶。

適用範圍包括但不限於：UI、CSS、版面、美術圖片、角色頁、背包、裝備、
技能、戰鬥介面、彈窗、商店、任務、導航、圖示、動畫及響應式行為。

除非使用者明確要求更新本文件，否則不得跳過、弱化或以「統一風格」為由
違反以下規範。

---

## 一、目前既有 UI 不全面重做

目前遊戲已經有大量 UI，因此：

- 不要一次重寫全部 UI。
- 不要因為統一風格而破壞現有功能。
- 採取「逐頁整理、逐元件替換」。
- 舊頁面可以暫時保留。
- 新增頁面必須直接遵守本規範。
- 修改舊頁面時，再逐步統一。

如果只是改 UI，不要順便改戰鬥、存檔、數值、掉落等遊戲邏輯。

## 二、整體手機畫面規則

遊戲主要以手機直式為主。

核心 UI 設計比例為 **9:16**，建議設計基準為 **1080 × 1920**。

但 9:16 只是核心設計比例，不是把所有手機強制拉成 9:16。應採用：

**9:16 核心安全區 + 不同手機背景延伸**

長螢幕手機多出的空間優先由背景、留白處理，不得把 UI 整體硬拉長。

## 三、不要把所有東西都做成 9:16

只有整體頁面以 9:16 為基準。以下通常維持 1:1：

- 裝備 Icon
- 技能 Icon
- 道具 Icon
- 背包格
- 頭像
- 導航 Icon

人物立繪、武器、裝備 PNG 必須保持原始比例。HP、SP、EXP 等維持橫向長條。

## 四、圖片禁止變形

所有圖片都必須保持原始比例：

- 背景使用 `cover`。
- 人物、裝備、Icon 使用 `contain`。
- 不得為了填滿容器而強制拉寬或拉高。

不得出現：

- 人物被拉胖
- 武器被拉長
- Icon 變形
- 裝備被裁切
- 圖片超出容器

## 五、UI 必須元件化

不要每個頁面各寫一套相似 UI。應建立共用元件，例如：

- `Panel`
- `ItemSlot`
- `EquipmentSlot`
- `SkillSlot`
- `Button`
- `TitleBar`
- `Dialog`
- `Tooltip`
- `ProgressBar`
- `BottomNav`
- `Tab`
- `Badge`

相同功能必須共用同一套元件。例如背包格、商店格、掉落預覽格，優先共用
`ItemSlot`，再透過不同狀態改變外觀。

## 六、建立統一 UI 狀態

元件應支援統一狀態，例如：

- `normal`
- `selected`
- `disabled`
- `locked`
- `active`
- `equipped`
- `rare`
- `legendary`

不要每個頁面自行重新設計選取框與啟用效果。

## 七、建立 UI Tokens

顏色、間距、字級、圓角、陰影等必須集中管理。建議起始結構：

```css
:root {
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 12px;
  --space-lg: 16px;
  --space-xl: 24px;

  --ui-bg: #17130f;
  --ui-gold: #c89a48;
  --ui-text: #f4ead4;
  --ui-text-muted: #a99b83;

  --radius-sm: 6px;
  --radius-md: 10px;
}
```

後續不要到處散落不同但很接近的顏色與尺寸。新增 token 前先確認現有 token
是否已能表達同一用途。

## 八、降低「網頁方塊感」

UI 不要大量使用普通矩形卡片。遊戲 UI 可適度使用：

- SVG
- `clip-path`
- `mask`
- `border-image`
- `::before`
- `::after`
- 不規則邊角
- 金屬裝飾
- 紋理
- 半透明層
- 元件重疊
- 光影層次

但不要為了華麗造成可讀性下降。

## 九、美術框優先考慮可重用

大型 UI 框不要每種尺寸都畫一張。優先使用 **9-slice / Nine-slice**，讓同一個
美術框可以套用到：

- 背包
- 裝備
- 任務
- 彈窗
- 商店
- 技能資訊

避免未來修改一個風格就要重新製作大量圖片。

## 十、技術分工

### HTML / CSS

負責主要版面、文字、按鈕、背包、裝備、捲動、彈窗。

### SVG

負責特殊框線、不規則造型、裝飾、圖形。

### Canvas

負責粒子、火星、雪花、風、煙霧、抽獎動畫。

### WebGL / Shader

只用於少量重要效果，例如：

- 傳說裝備
- SSR
- Boss
- 大招
- 特殊背景

不要把整個 UI 都改成 Canvas 或 WebGL。

## 十一、動畫規則

動畫應統一節奏：

- 一般按鈕：100～150ms
- 頁籤：150～200ms
- 彈窗：200～300ms
- 大型特殊動畫：依效果另外設計

避免不同頁面的相同互動使用完全不同的動畫速度。

## 十二、效能規則

裝備格、背包格等大量出現的元件，不要每格都使用高成本 Shader 或大量 Canvas
特效。大量列表以 **CSS + 圖片** 為主；Canvas / Shader 只用於目前畫面中少數
重點元素。

## 十三、手機響應式規則

優先使用：

- `flex`
- `grid`
- `%`
- `vw`
- `vh`
- `clamp()`
- `aspect-ratio`
- `max-width`
- `max-height`

避免大量使用：

- 固定座標
- `absolute` 定位
- `translate` 補位置
- 每台手機一套 Media Query

Media Query 可以使用，但不能變成針對各手機型號的補丁集合。

## 十四、安全區

需考慮：

- `safe-area-inset-top`
- `safe-area-inset-bottom`
- 狀態列
- 瀏海
- 手勢導航區

重要按鈕不得貼死螢幕邊緣。

## 十五、底部導航

底部導航應維持固定且一致，不得因不同頁面：

- 上下跳動
- 被內容推走
- 被遮住
- 和內容重疊

## 十六、美術系列一致性

同一系列裝備必須維持一致視覺語言：

- **赤炎**：黑鐵、赤紅、熔岩橘、金色、火焰、紅晶石。
- **寒泉**：冰藍、寒泉青、銀白、冰晶、水流。
- **青嵐**：青綠、翠青、銀白、風、氣流。
- **岩岳**：土黃、岩褐、暗金、岩石、山岳、岩晶。

同系列武器、鎧甲、袍、鞋、頭部、護腕、法環必須看得出來屬於同一套。

## 十七、不要把文字畫死在圖片裡

背景、美術框負責：

- 裝飾
- 材質
- 框架
- 視覺風格

HTML / CSS 負責：

- 文字
- 數值
- 按鈕
- 道具
- 裝備
- 技能
- 動態資料

避免整張 UI 直接變成一張圖片。

## 十八、修改 UI 時禁止順手改無關功能

- 如果需求是「更換 Icon」，只能修改相關 Icon。
- 如果需求是「修改背包 UI」，不要順便修改背包掉落邏輯。
- 如果需求是「修改裝備欄」，不要順便修改裝備能力計算。

UI 與遊戲邏輯盡量分離。UI 任務若發現邏輯問題，應記錄並另外處理，不得在
沒有明確授權時夾帶修改。

## 十九、目前舊 UI 的整理方式

不要一次全面改版，採用以下流程：

1. 建立全域 UI 規範。
2. 建立 UI Tokens。
3. 建立共用元件。
4. 先改角色／裝備頁。
5. 再改背包頁。
6. 驗證元件是否可以重用。
7. 再逐步套用技能、任務、商店等頁面。
8. 最後才處理戰鬥與特殊動畫。

## 二十、每次修改 UI 後檢查

每次 UI 修改至少確認：

1. UI 是否跑版。
2. 9:16 是否正常。
3. 長螢幕是否正常。
4. 圖片有沒有變形。
5. 圖片有沒有被裁切。
6. 文字是否重疊。
7. 按鈕是否可點擊。
8. 捲動是否正常；位於 `#game-stage` 的新捲動區也必須檢查觸控鎖白名單。
9. 彈窗是否超出畫面。
10. 導航列是否正常。
11. 舊功能是否正常。
12. Console 是否新增錯誤。

## 最重要的原則

不要一次推翻目前所有 UI。

從現在開始建立統一規則；舊 UI 在未來修改時逐步整理，新 UI 必須直接遵守
本規範。

核心方向：

**統一規則 → 共用元件 → 少量頁面驗證 → 再大量套用**

目標是讓遊戲逐漸脫離「普通網頁模組方塊感」，形成統一且具有正式 RPG
質感的 UI 系統，同時避免未來修改成本越來越高。

