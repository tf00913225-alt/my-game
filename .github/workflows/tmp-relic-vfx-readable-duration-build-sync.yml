name: Temporary relic VFX readable duration build sync

on:
  push:
    branches:
      - fix/relic-vfx-readable-duration-20260919
    paths:
      - .github/workflows/tmp-relic-vfx-readable-duration-build-sync.yml

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout work branch
        uses: actions/checkout@v4
        with:
          ref: fix/relic-vfx-readable-duration-20260919
          fetch-depth: 0

      - name: Build production bundles
        run: |
          npm run build
          git diff --check

      - name: Commit synchronized build
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add asset-manifest.json build
          if git diff --cached --quiet; then
            echo "Build already synchronized"
          else
            git commit -m "build: sync readable relic VFX duration bundles"
            git push origin HEAD:fix/relic-vfx-readable-duration-20260919
          fi
