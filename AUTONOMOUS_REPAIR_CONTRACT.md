# 《四象江湖傳》受控自主修復契約

> 文件定位：本文件是《四象江湖傳》所有「修復型任務」的唯一自主操作契約來源。
>
> 版本：v1.0
>
> 生效日期：2026-09-17
>
> 適用 Repository（程式庫）：`tf00913225-alt/my-game`

---

## 1. 契約目的

本契約的目的，是讓後續任何新對話、Codex（程式代理）、GPT（生成式預訓練模型）或其他開發代理，在收到《四象江湖傳》的修復型任務時，不需要使用者重貼整段規則，也能依固定邊界自主完成低風險修復。

本契約只授權「受控自主修復」，不是一般性的自由修改授權。

---

## 2. 自動觸發條件

只要使用者的任務明確屬於下列任一類型，且沒有在當次對話另行指定不同模式，就預設啟用本契約：

- 修復 Bug（錯誤）
- `fix`（修復）
- 處理 failure（失敗）
- 修正 test failure（測試失敗）
- 修正 CI failure（持續整合失敗）
- 修正 fixture（測試夾具）
- 修正 test harness（測試框架）
- 修正 stale contract（過期測試契約）
- 處理既有修復分支上的下一個失敗

若任務是新增功能、重構 production runtime（正式執行程式）、改玩法規格、改正式資料、改 UI（使用者介面）設計或其他非修復型工作，不得因本文件而自動取得自主修改授權。

---

## 3. 開工前的強制讀取順序

開始任何自主修復前，必須先確認 GitHub（程式庫）當下最新 `dev`（開發分支），並閱讀：

1. `AGENTS.md`
2. `CLAUDE.md`
3. `HANDOFF.md`
4. `ARCHITECTURE_RULES.md`
5. `UI_GUIDELINES.md`（若任務涉及 UI／版面／視覺）
6. `AUTONOMOUS_REPAIR_CONTRACT.md`（本文件）

不得只依賴舊對話摘要、記憶中的 SHA（提交雜湊）或過去的工作分支狀態。

---

## 4. 分支與基準規則

自主修復開始時：

- 以 GitHub 當下最新 `dev`（開發分支）為正式整合基準。
- 必須建立或使用明確指定的獨立 `fix/...` 修復分支。
- 禁止直接修改 `main`（正式分支）。
- 禁止直接修改 `dev`（開發分支）。
- 禁止直接修改任何被指定為 production（正式）或保護中的工作分支。
- 禁止 `rebase`（重定基底）。
- 禁止 `force push`（強制推送）。
- 禁止為了讓測試通過而自行合併 `main`、`dev` 或其他正式分支進修復分支。

若使用者在當次任務已指定精確 base SHA（基準提交雜湊）或工作分支，依該次明確指令執行；不得自行換基準。

---

## 5. 預設自主授權範圍

預設只允許處理低風險、非 production runtime（正式執行程式）的修復，包含：

- `tests-only`（純測試）修改
- fixture（測試夾具）修正
- test harness（測試框架）修正
- stale contract（過期測試契約）修正
- 測試資料與正式既有規格不一致時，將測試資料對齊既有正式規格
- 測試仍依賴已淘汰 DOM（文件物件模型）、舊幾何、舊 selector（選擇器）或舊測試入口時的最小修正
- B2、E、E+B2 類型中，經證據確認屬於上述非 production、tests-only（純測試）範圍的問題

B2／E／E+B2 只是既有分類標籤，**分類名稱本身不得被當成擴張修改權限的理由**。只要實際修正會碰到 production runtime（正式執行程式），就不在預設自主授權內。

---

## 6. 標準自主循環

在允許範圍內，每一個 failure（失敗）可直接執行：

**診斷 → 蒐證 → 分類 → 最小修正 → 重跑精準測試 → 取得可執行驗證 → 記錄結果 → 再處理下一個 failure（失敗）**

在符合本契約的情況下，不需要每一個 failure（失敗）都停下來向使用者請示。

但「測試看起來應該會過」、「靜態閱讀認為正確」、「只比對程式碼」都不算完成驗證。

---

## 7. 可執行驗證門檻

任何 failure（失敗）在進入下一個不同 failure（失敗）之前，必須先取得與該問題直接相關的 executable verification（可執行驗證）。

可接受的驗證依任務而定，例如：

- 對應 test（測試）實際執行並通過
- 對應 test suite（測試套件）實際執行並通過
- 必要的 CI（持續整合）工作實際執行並通過

如果唯一允許的驗證方式無法真正執行，例如 CI（持續整合）無法形成可執行的 merge snapshot（合併快照），不得為了強行取得結果去：

- 合併 `main`
- 合併 `dev`
- `rebase`（重定基底）
- 修改 workflow（工作流程）
- 修改 production runtime（正式執行程式）

此時必須停止並回報阻塞證據。

---

## 8. 自主 failure（失敗）額度

每一輪受控自主修復最多處理 **4 個不同 failure（失敗）**。

規則如下：

- Failure 1～4：若都在授權範圍內，可以依自主循環連續處理。
- 第 5 個不同 failure（失敗）：必須停止，不得自行開始修復。
- 同一個 failure（失敗）最多自主修正 **2 次**。
- 同一 failure（失敗）第 2 次仍無法取得可執行驗證：必須停止。
- 不得透過改名、拆分、重新建立分支或重新開啟對話來重置已使用的額度。
- 若是同一修復任務／同一工作分支的續跑，額度沿用前一輪已使用數量，除非使用者明確開啟新一輪。

額度是上限，不代表必須用滿；任何硬停止條件一旦成立，立即停止。

---

## 9. CI Validation Draft PR（持續整合驗證草稿合併請求）

只有在本機／現有工具無法取得必要的 CI（持續整合）驗證，而且建立 PR（合併請求）是唯一合理驗證入口時，允許每一輪最多建立 **1 個** CI Validation Draft PR（持續整合驗證草稿合併請求）。

此 PR（合併請求）必須：

- 只用於驗證。
- 維持 Draft（草稿）狀態。
- 以使用者指定的整合分支為 base；未指定時以 `dev`（開發分支）為 base。
- 不得合併。
- 不得藉由它修改 workflow（工作流程）。
- 不得藉由它引入 production runtime（正式執行程式）修改。
- 若無法形成可執行 merge snapshot（合併快照），立即停止，不得採用高風險替代手段。

此授權只為取得驗證，不是整合授權。

---

## 10. 絕對硬停止條件

出現以下任一情況，立即停止自主修復並回報，不得自行猜規格或擴大修改：

1. 需要修改 production runtime（正式執行程式）。
2. 需要修改正式玩法、傷害、技能、狀態、掉落、經濟、帳號、存檔或 Firebase（雲端服務）正式行為。
3. 需要修改 workflow（工作流程）、CI（持續整合）定義或部署流程才能讓測試通過。
4. 正式規格不明確，存在兩種以上合理解讀。
5. 證據不足，無法證明問題是 tests-only（純測試）／fixture（測試夾具）／test harness（測試框架）／stale contract（過期契約）。
6. 修正會改變玩家可觀察到的正式行為。
7. 修正可能影響存檔相容性、UID（使用者識別碼）、帳號、雲端資料或高風險資料。
8. 必須碰 `main`（正式分支）或直接寫入 `dev`（開發分支）。
9. 必須 `rebase`（重定基底）、`force push`（強制推送）或改寫歷史。
10. 已達本輪 4 個不同 failure（失敗）的自主額度。
11. 同一 failure（失敗）已自主修正 2 次仍未取得可執行驗證。
12. 目前 failure（失敗）尚未完成可執行驗證，卻只能靠跳到下一個 failure（失敗）才能繼續。
13. 唯一允許的 CI Validation Draft PR（持續整合驗證草稿合併請求）無法形成可執行驗證。

停止時應保留工作分支現況，不得為了「收尾」而進一步修改正式程式。

---

## 11. 禁止用測試掩蓋正式錯誤

本契約允許修正過期測試，但不允許「為了綠燈而改測試」。

如果 production runtime（正式執行程式）與正式規格本身有誤，測試正確地抓到該錯誤時：

- 不得刪除 assertion（斷言）。
- 不得放寬 assertion（斷言）。
- 不得跳過測試。
- 不得把失敗改成永遠通過。
- 不得把正式錯誤重新定義成 fixture（測試夾具）問題。

應立即停止，將問題標記為 production-risk（正式程式風險）交回使用者決定。

---

## 12. 最小修改原則

每次自主修復只能修改完成當前 failure（失敗）所需的最小範圍。

禁止順手：

- 重構無關程式
- 改 UI（使用者介面）
- 改命名
- 格式化整個檔案
- 升版本
- 改 cache key（快取鍵）
- 改 production asset（正式素材）
- 清理其他技術債
- 修正下一個尚未實際出現的假設問題

若看到額外問題，記錄即可，不得擴大 scope（範圍）。

---

## 13. 每次提交前的檢查

在 commit（提交）前至少確認：

- 變更檔案全部位於本次允許範圍。
- 沒有 production runtime（正式執行程式）檔案混入。
- 沒有 workflow（工作流程）變更。
- 沒有意外版本號／cache key（快取鍵）變更。
- 沒有 `main`／`dev` 直接修改。
- 對應 executable verification（可執行驗證）已實際執行。

若其中任何一項無法確認，不得提交，改為停止回報。

---

## 14. 每輪回報格式

受控自主修復停止或完成時，至少回報：

- 工作分支名稱
- 起始 base SHA（基準提交雜湊）
- 最終 HEAD SHA（分支頂端提交雜湊）
- 已使用 failure（失敗）額度，例如 `2 / 4`
- 每個 failure（失敗）的分類與根因
- 修改檔案
- 實際執行的驗證與結果
- 是否碰觸 production runtime（正式執行程式）：必須明確回答
- 停止原因（若有）
- 下一步建議，但不得自行跨越停止線

---

## 15. 規則優先序

本文件負責定義「修復任務可以自主做到哪裡」。

其他專案文件仍各自負責正式規格與架構：

- `ARCHITECTURE_RULES.md`：架構與高風險系統規則
- `UI_GUIDELINES.md`：UI（使用者介面）規則
- `HANDOFF.md`：目前專案狀態與交接
- `AGENTS.md`／`CLAUDE.md`：代理入口與工作守則

若文件間出現衝突：

1. 當次使用者的明確指令優先。
2. 但不得把模糊語句解讀成擴大自主權限。
3. 涉及自主修復邊界時，以本契約較嚴格的停止條件為準。
4. 涉及正式遊戲規格／架構時，以對應正式規範文件為準。
5. 無法確定時，停止，不猜。

---

## 16. 新對話的預期行為

未來新對話中，使用者只需要說例如：

> 幫我修《四象江湖傳》這個 Bug（錯誤），依目前正式規範執行。

代理就應自行：

1. 讀取最新 GitHub（程式庫）狀態。
2. 讀取本契約與永久規範。
3. 建立／確認安全修復分支。
4. 在本契約允許範圍內自主修復。
5. 觸及硬停止條件才停止回報。

**使用者不需要再次複製本契約全文。**
