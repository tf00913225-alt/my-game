name: Temporary shared nav VFX battle layout repair
on:
  push:
    branches: [fix/shared-nav-vfx-battle-layout-20260914]
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/shared-nav-vfx-battle-layout-20260914
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Patch true owners
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import re

          def read(path):
              return Path(path).read_text(encoding='utf-8')
          def write(path, text):
              Path(path).write_text(text, encoding='utf-8')
          def sub_once(path, pattern, replacement, flags=re.S):
              text=read(path)
              new,count=re.subn(pattern,replacement,text,count=1,flags=flags)
              assert count==1, f'{path}: pattern did not match exactly once: {pattern[:80]}'
              write(path,new)
          def replace_once(path, old, new):
              text=read(path)
              assert text.count(old)==1, f'{path}: expected exactly one occurrence of {old[:80]!r}, got {text.count(old)}'
              write(path,text.replace(old,new,1))

          # 1) Retire earlier dungeon-nav markup owners. They may request a sync,
          #    but V148 is now the only renderer/markup owner for both Gameplay and Dungeon.
          sub_once(
              'js/35-v141-ui-battle.js',
              r'    function installDungeonNavigation\(\)\{.*?\n    \}\n\n    /\* =====================================================\n       Notification dots',
              '''    function installDungeonNavigation(){
        /* The legacy v141-dungeon-return/nav markup owner is retired.
           The final V148 context-nav owner creates and renders the shell. */
        if(typeof window.v148SyncContextNavigation==="function"){
            window.v148SyncContextNavigation();
        }else if(typeof window.v148SyncDungeonShell==="function"){
            window.v148SyncDungeonShell();
        }
    }

    /* =====================================================
       Notification dots'''
          )

          sub_once(
              'js/41-v146-system-polish.js',
              r'\n    function dungeonNavMarkup\(abyssMapActive,abyssSelectionActive\)\{.*?\n    \}\n\n    window\.v146ExitAbyssMap=',
              '\n    window.v146ExitAbyssMap='
          )
          sub_once(
              'js/41-v146-system-polish.js',
              r'        let nav=document\.getElementById\("v141DungeonNav"\);.*?        const oldReturn=',
              '''        const nav=document.getElementById("v141DungeonNav");
        if(nav){ nav.dataset.v146Columns="5"; }
        if(typeof window.v148SyncContextNavigation==="function"){
            window.v148SyncContextNavigation();
        }else if(typeof window.v148SyncDungeonShell==="function"){
            window.v148SyncDungeonShell();
        }
        const oldReturn='''
          )

          # 2) V148 owns one shared five-button renderer for Gameplay + Dungeon.
          sub_once(
              'js/42-v148-combat-dungeon-fixes.js',
              r'    /\* ----- Dungeon navigation and movement\. ----- \*/.*?    function scheduleDungeonSync\(\)\{ setTimeout\(syncDungeonShell,0\); \}',
              '''    /* ----- Shared Gameplay / Dungeon navigation and movement. ----- */
    const CONTEXT_NAV_ITEMS=Object.freeze([
        Object.freeze(["角色","assets/ui/nav-character.png","openHomeFeature('character')"]),
        Object.freeze(["背包","assets/ui/nav-backpack.png","openMapInventoryOverlay()"]),
        Object.freeze(["秘寶","assets/ui/nav-relic-v175.webp","openHomeFeature('relic')"]),
        Object.freeze(["元素匣","assets/ui/nav-element-box.png","openHomeFeature('autoBattleSettings')"])
    ]);

    function contextNavMarkup(returnAction){
        const buttons=CONTEXT_NAV_ITEMS.map(item=>item.slice());
        buttons.push(["返回","assets/ui/map-return.png",returnAction]);
        return buttons.map(button=>'<button class="nav-button nav-art-button-wrap" onclick="'+button[2]+'" aria-label="'+button[0]+'"><img class="nav-art-button" src="'+button[1]+'" alt=""><span class="nav-sr-only">'+button[0]+'</span></button>').join("");
    }

    function contextNavMatches(nav,returnAction){
        const buttons=Array.from(nav&&nav.children||[]);
        if(buttons.length!==5){ return false; }
        const labels=buttons.map(button=>button&&typeof button.getAttribute==="function"?button.getAttribute("aria-label"):"").join("|");
        if(labels!=="角色|背包|秘寶|元素匣|返回"){ return false; }
        const relicImage=buttons[2]&&typeof buttons[2].querySelector==="function"?buttons[2].querySelector("img"):null;
        if(!relicImage||relicImage.getAttribute("src")!=="assets/ui/nav-relic-v175.webp"){ return false; }
        const action=buttons[4]&&typeof buttons[4].getAttribute==="function"?buttons[4].getAttribute("onclick"):"";
        return action===returnAction;
    }

    function renderContextNav(nav,returnAction,mode){
        if(!nav){ return; }
        if(nav.dataset.v148Mode!==mode||!contextNavMatches(nav,returnAction)){
            nav.innerHTML=contextNavMarkup(returnAction);
            nav.dataset.v148Mode=mode;
        }
        nav.dataset.v146Columns="5";
    }

    function dungeonReturnAction(abyssMapActive,abyssSelectionActive){
        return abyssMapActive
            ?(typeof window.v174AbyssBackToSelection==="function"?"v174AbyssBackToSelection()":"v146ExitAbyssMap()")
            :(abyssSelectionActive?"v174AbyssLeaveToGameplay()":"showPage('home')");
    }

    function activeGameplayPageId(){
        const ids=["gameplayPage","bossPage","towerPage"];
        for(const id of ids){
            const page=document.getElementById(id);
            if(page&&page.classList&&page.classList.contains("active")){ return id; }
        }
        return "";
    }

    window.v148ReturnFromGameplay=function(){
        const activeId=activeGameplayPageId();
        if(activeId&&activeId!=="gameplayPage"&&typeof window.vGameplayBackToHub==="function"){
            window.vGameplayBackToHub();
        }else if(typeof showPage==="function"){
            showPage(activeId&&activeId!=="gameplayPage"?"gameplay":"home");
        }
    };

    function syncContextNavigation(){
        if(typeof document==="undefined"){ return; }
        const page=document.getElementById("dungeonPage");
        const app=document.getElementById("app");
        const gameplayPageId=activeGameplayPageId();
        const dungeonActive=!!(page&&page.classList&&page.classList.contains("active"));
        const gameplayActive=!!gameplayPageId;
        const contextActive=dungeonActive||gameplayActive;
        if(app&&app.classList&&typeof app.classList.toggle==="function"){
            app.classList.toggle("v148-context-nav-active",contextActive);
        }

        const abyssMapActive=!!(dungeonActive&&page.querySelector(".v141-abyss-shell"));
        const abyssSelectionActive=!!(dungeonActive&&page.querySelector(".v174-abyss-selection,.v174-abyss-complete,.v141-abyss-intro"));
        let topReturn=document.getElementById("v146AbyssReturn");
        if(abyssMapActive&&!topReturn&&typeof document.createElement==="function"){
            topReturn=document.createElement("button");
            topReturn.id="v146AbyssReturn";
            topReturn.type="button";
            topReturn.className="v146-abyss-return";
            topReturn.setAttribute("aria-label","返回上一層");
            topReturn.innerHTML='<img src="assets/ui/map-return.png" alt="">';
            topReturn.onclick=window.v146ExitAbyssMap;
            page.appendChild(topReturn);
        }else if(!abyssMapActive&&topReturn){
            topReturn.remove();
        }

        let nav=document.getElementById("v141DungeonNav");
        if(contextActive&&!nav&&typeof document.createElement==="function"){
            nav=document.createElement("div");
            nav.id="v141DungeonNav";
            nav.className="bottom-nav map-page-nav v141-dungeon-nav v148-context-nav";
            const owner=document.getElementById("game-content")||app;
            if(owner&&typeof owner.appendChild==="function"){ owner.appendChild(nav); }
        }
        if(!nav||!contextActive){ return; }

        const returnAction=gameplayActive&&!dungeonActive
            ?"v148ReturnFromGameplay()"
            :dungeonReturnAction(abyssMapActive,abyssSelectionActive);
        const mode=gameplayActive&&!dungeonActive
            ?"gameplay:"+gameplayPageId
            :(abyssMapActive?"abyss-map":(abyssSelectionActive?"abyss-selection":"daily"));
        renderContextNav(nav,returnAction,mode);
    }

    function scheduleDungeonSync(){ setTimeout(syncContextNavigation,0); }'''
          )
          p='js/42-v148-combat-dungeon-fixes.js'; s=read(p)
          s=s.replace('syncDungeonShell','syncContextNavigation')
          s=s.replace('            if(page==="dungeon"){ scheduleDungeonSync(); }','            scheduleDungeonSync();',1)
          s=s.replace('    window.v148SyncDungeonShell=syncContextNavigation;','    window.v148SyncContextNavigation=syncContextNavigation;\n    window.v148SyncDungeonShell=syncContextNavigation;',1)
          assert 'window.v148SyncContextNavigation=syncContextNavigation;' in s
          write(p,s)

          # Shared context nav uses the exact same visual shell in Gameplay and Dungeon;
          # the ordinary five-destination global nav is hidden only while that shell is active.
          replace_once(
              'css/38-v141-system-expansion.css',
              '''#game-stage #app:not(.v141-dungeon-active) #v141DungeonNav{display:none !important;}
#game-stage #app.v141-dungeon-active #v141DungeonNav{display:flex !important;}
#game-stage #app.v141-dungeon-active #bottomNav{display:none !important;}''',
              '''#game-stage #app:not(.v141-dungeon-active):not(.v148-context-nav-active) #v141DungeonNav{display:none !important;}
#game-stage #app.v141-dungeon-active #v141DungeonNav,
#game-stage #app.v148-context-nav-active #v141DungeonNav{display:flex !important;}
#game-stage #app.v141-dungeon-active #bottomNav,
#game-stage #app.v148-context-nav-active #bottomNav{display:none !important;}'''
          )

          # 3) Formal VFX: the stage/positioning node is allowed to overflow; only
          #    the inner frame viewport clips one Sprite Sheet cell.
          sub_once(
              'js/39-v143-skill-animation.js',
              r'    function appendSpriteNode\(current\)\{.*?        return node;\n    \}',
              '''    function appendSpriteNode(current){
        const node=document.createElement("i");
        node.className="v143-vfx-sprite";
        node.dataset.skill=current.config.id||"unknown";
        node.dataset.targetSide=current.targetSide;
        node.dataset.renderer="dom-sprite";
        node.dataset.confirmedHit="false";
        node.style.visibility="hidden";
        const frame=document.createElement("i");
        frame.className="v143-vfx-frame";
        frame.dataset.renderer="dom-sprite-frame";
        if(typeof frame.setAttribute==="function"){ frame.setAttribute("aria-hidden","true"); }
        node.appendChild(frame);
        node.__v143Frame=frame;
        state.stage.appendChild(node);
        return node;
    }

    function spriteFrameNode(node){
        return node&&node.__v143Frame?node.__v143Frame:node;
    }'''
          )
          replace_once(
              'js/39-v143-skill-animation.js',
              '''            node.dataset.frames=String(sprite.frames);
            node.style.backgroundImage='url("'+String(sprite.src).replace(/"/g,"%22")+'")';
            node.style.backgroundSize=(sprite.columns*100)+"% "+(sprite.rows*100)+"%";
            node.style.setProperty("--v143-sprite-duration",current.duration+"ms");''',
              '''            node.dataset.frames=String(sprite.frames);
            const frame=spriteFrameNode(node);
            const imageValue='url("'+String(sprite.src).replace(/"/g,"%22")+'")';
            const sizeValue=(sprite.columns*100)+"% "+(sprite.rows*100)+"%";
            /* Keep the outer metadata styles for diagnostics/tests, but CSS prevents
               them from painting. The inner frame is the only Sprite Sheet viewport. */
            node.style.backgroundImage=imageValue;
            node.style.backgroundSize=sizeValue;
            frame.style.backgroundImage=imageValue;
            frame.style.backgroundSize=sizeValue;
            node.style.setProperty("--v143-sprite-duration",current.duration+"ms");'''
          )

          replace_once(
              'css/40-v143-combat-dungeon-polish.css',
              '''.v143-skill-stage{
    position:fixed;
    inset:0;
    z-index:16000;
    overflow:hidden;
    contain:strict;
    pointer-events:none;
}''',
              '''.v143-skill-stage{
    position:fixed;
    inset:0;
    z-index:16000;
    overflow:visible;
    contain:layout style;
    pointer-events:none;
}'''
          )
          replace_once(
              'css/40-v143-combat-dungeon-polish.css',
              '''.v143-vfx-sprite{
    position:fixed;
    display:block;
    box-sizing:border-box;
    background-position:0 0;
    background-repeat:no-repeat;
    image-rendering:auto;
    opacity:0;
    pointer-events:none;
    transform:translate(-50%,-50%);
    transform-origin:center;
    will-change:background-position,transform,opacity;
}''',
              '''.v143-vfx-sprite{
    position:fixed;
    display:block;
    box-sizing:border-box;
    overflow:visible;
    background-image:none !important;
    opacity:0;
    pointer-events:none;
    transform:translate(-50%,-50%);
    transform-origin:center;
    will-change:transform,opacity;
}

.v143-vfx-frame{
    position:absolute;
    inset:0;
    display:block;
    box-sizing:border-box;
    overflow:hidden;
    background-position:0 0;
    background-repeat:no-repeat;
    image-rendering:auto;
    pointer-events:none;
    will-change:background-position;
}'''
          )
          sub_once(
              'css/40-v143-combat-dungeon-polish.css',
              r'\.v143-vfx-sprite\.v143-vfx-sprite-active\{.*?\n\}\n\n\.v143-vfx-sprite\.v143-vfx-sprite-active\[data-travel="true"\]\{.*?\n\}',
              '''.v143-vfx-sprite.v143-vfx-sprite-active{
    animation:v143RasterCastEnvelope var(--v143-sprite-duration,1200ms) linear var(--v143-sprite-delay,0ms) 1 both;
}

.v143-vfx-sprite.v143-vfx-sprite-active > .v143-vfx-frame{
    animation:v143RasterCastFrames var(--v143-sprite-duration,1200ms) steps(1,end) var(--v143-sprite-delay,0ms) 1 both;
}

.v143-vfx-sprite.v143-vfx-sprite-active[data-travel="true"]{
    animation:v143RasterTravel var(--v143-sprite-duration,1200ms) linear var(--v143-sprite-delay,0ms) 1 both;
}'''
          )

          # Cards must never be the clipping boundary for cast/status artwork.
          replace_once(
              'css/40-v143-combat-dungeon-polish.css',
              '''    min-height:var(--v143-monster-card-height,100px) !important;
    padding:3px !important;
}''',
              '''    min-height:var(--v143-monster-card-height,100px) !important;
    padding:3px !important;
    overflow:visible !important;
}'''
          )
          # Reserve the same below-card identity lane that player cards already use.
          marker='''#game-stage > #app > #game-content #battlePage .battle-monster[data-rank="boss"]{'''
          s=read('css/40-v143-combat-dungeon-polish.css')
          assert marker in s
          s=s.replace(marker,'''
#game-stage #battleMonsterArea .v131-monster-row{
    min-height:120px;
    padding-bottom:18px;
    box-sizing:border-box;
    overflow:visible;
}

'''+marker,1)
          s=s.replace('font-size:var(--v143-monster-bar-font-size,12px) !important;','font-size:var(--v143-monster-bar-font-size,9px) !important;',1)
          write('css/40-v143-combat-dungeon-polish.css',s)

          # Enemy bar text follows the player's small 9px HUD typography instead of
          # forcing 13px inline text into the same 11px resource lane.
          p='js/38-v143-system-fixes.js'; s=read(p)
          old='''            node.style.setProperty("font-size","13px","important");
            const available=Math.max(1,node.clientWidth||68);
            let size=13;
            while(size>11&&node.scrollWidth>available){'''
          new='''            node.style.setProperty("font-size","9px","important");
            const available=Math.max(1,node.clientWidth||68);
            let size=9;
            while(size>8&&node.scrollWidth>available){'''
          assert s.count(old)==1
          write(p,s.replace(old,new,1))

          # 4) Final cardless presentation owner: player art +50%, enemy/BOSS art
          #    uses contain, enemy identity lives below HP/SP, bars use player-thin lane.
          p='js/54-v173.51-battle-qa.js'; s=read(p)
          s=s.replace(
              '''#game-stage > #app > #game-content #battlePage .battle-player.v174-cardless-unit>.v174-battle-art{
    inset:-2px -2px 30px!important;
    background-size:contain!important;background-position:center bottom!important;
}''',
              '''#game-stage > #app > #game-content #battlePage .battle-player.v174-cardless-unit>.v174-battle-art{
    /* 122×94 legacy art box -> ~183×141 visual box: exact 1.5x presentation. */
    inset:-49px -33px 30px!important;
    background-size:contain!important;background-position:center bottom!important;
}''',1)
          old_name='''#game-stage > #app > #game-content #battlePage .battle-monster.v174-cardless-unit>.battle-monster-name{
    position:absolute!important;left:0!important;right:0!important;top:0!important;
    display:flex!important;align-items:center!important;justify-content:center!important;
    min-height:14px!important;height:14px!important;margin:0!important;padding:0 2px!important;
    white-space:nowrap!important;overflow:visible!important;visibility:visible!important;opacity:1!important;
    z-index:24!important;pointer-events:none!important;
}
#game-stage > #app > #game-content #battlePage .battle-monster.v174-cardless-unit>.v174-battle-art{
    inset:15px -5px 26px!important;
    background-size:cover!important;background-position:center center!important;
}'''
          new_name='''#game-stage > #app > #game-content #battlePage .battle-monster.v174-cardless-unit>.battle-monster-name{
    position:absolute!important;left:-4px!important;right:-4px!important;top:calc(100% + 3px)!important;
    display:flex!important;align-items:center!important;justify-content:center!important;
    min-height:16px!important;height:16px!important;margin:0!important;padding:0 2px!important;
    white-space:nowrap!important;overflow:visible!important;visibility:visible!important;opacity:1!important;
    z-index:24!important;pointer-events:none!important;
}
#game-stage > #app > #game-content #battlePage .battle-monster.v174-cardless-unit>.v174-battle-art{
    inset:2px 2px 26px!important;
    background-size:contain!important;background-position:center center!important;
}
#game-stage > #app > #game-content #battlePage .battle-monster.gameplay-boss-card.v174-cardless-unit>.v174-battle-art{
    inset:2px 2px 26px!important;
    background-size:contain!important;background-position:center center!important;
}
#game-stage > #app > #game-content #battlePage .battle-monster.v174-cardless-unit>.monster-hp,
#game-stage > #app > #game-content #battlePage .battle-monster.v174-cardless-unit>.monster-sp{
    height:11px!important;min-height:11px!important;border-radius:4px!important;
}
#game-stage > #app > #game-content #battlePage .battle-monster.v174-cardless-unit>.monster-hp>.monster-bar-text,
#game-stage > #app > #game-content #battlePage .battle-monster.v174-cardless-unit>.monster-sp>.monster-bar-text{
    height:11px!important;font-size:9px!important;line-height:11px!important;
}'''
          assert s.count(old_name)==1
          write(p,s.replace(old_name,new_name,1))

          p='css/gameplay-boss-tower.css'; s=read(p)
          s=s.replace('--v143-monster-bar-font-size:9.5px;','--v143-monster-bar-font-size:9px;',1)
          old='''    box-sizing:border-box;
    align-self:start;
    overflow:hidden;
}'''
          new='''    box-sizing:border-box;
    align-self:start;
    overflow:visible;
}'''
          assert s.count(old)>=1
          # Only the Gameplay BOSS card block contains this exact local sequence first in this region.
          boss_index=s.index('#game-stage > #app > #game-content #battlePage .battle-monster.gameplay-boss-card[data-rank="boss"]')
          tail=s[boss_index:]
          assert old in tail
          tail=tail.replace(old,new,1)
          s=s[:boss_index]+tail
          write(p,s)

          # Keep historical tests aligned with the new final owners.
          p='tests/v148-combat-dungeon-fixes.test.js'; s=read(p)
          s=s.replace(
              'const page=element({querySelector:selector=>selector===".v141-abyss-shell"?{}:null});',
              'const page=element({classList:classList(["active"]),querySelector:selector=>selector===".v141-abyss-shell"?{}:null});'
          )
          write(p,s)

          p='tests/v143-combat-dungeon-polish.test.js'; s=read(p)
          s=s.replace('font-size:var\\(--v143-monster-bar-font-size,12px\\) !important;','font-size:var\\(--v143-monster-bar-font-size,9px\\) !important;')
          write(p,s)

          p='tests/remaining-ui-regressions-20260914.test.js'; s=read(p)
          s=s.replace('assert.match(finalNav,/function dungeonNavMatches\\(nav,abyssMapActive,abyssSelectionActive\\)/);','assert.match(finalNav,/function contextNavMatches\\(nav,returnAction\\)/);')
          s=s.replace('assert.match(finalNav,/buttons\\.push\\(\\["返回","assets\\/ui\\/map-return\\.png",dungeonReturnAction\\(abyssMapActive,abyssSelectionActive\\)\\]\\)/);','assert.match(finalNav,/buttons\\.push\\(\\["返回","assets\\/ui\\/map-return\\.png",returnAction\\]\\)/);')
          write(p,s)

          # New focused regression guard for this repair.
          Path('tests/shared-nav-vfx-battle-layout-20260914.test.js').write_text(r'''const assert=require("node:assert/strict");
const fs=require("node:fs");
const read=path=>fs.readFileSync(path,"utf8");

const early=read("js/35-v141-ui-battle.js");
const polish=read("js/41-v146-system-polish.js");
const finalNav=read("js/42-v148-combat-dungeon-fixes.js");
const navCss=read("css/38-v141-system-expansion.css");
const vfx=read("js/39-v143-skill-animation.js");
const vfxCss=read("css/40-v143-combat-dungeon-polish.css");
const battle=read("js/54-v173.51-battle-qa.js");
const bossCss=read("css/gameplay-boss-tower.css");
const enemy=read("js/38-v143-system-fixes.js");

assert.doesNotMatch(early,/function installDungeonNavigation\(\)[\s\S]*?nav\.innerHTML=/);
assert.doesNotMatch(polish,/function dungeonNavMarkup\(/);
assert.match(finalNav,/function contextNavMarkup\(returnAction\)/);
assert.match(finalNav,/\["角色","assets\/ui\/nav-character\.png"/);
assert.match(finalNav,/\["背包","assets\/ui\/nav-backpack\.png"/);
assert.match(finalNav,/\["秘寶","assets\/ui\/nav-relic-v175\.webp"/);
assert.match(finalNav,/\["元素匣","assets\/ui\/nav-element-box\.png"/);
assert.match(finalNav,/buttons\.push\(\["返回","assets\/ui\/map-return\.png",returnAction\]\)/);
assert.match(finalNav,/\["gameplayPage","bossPage","towerPage"\]/);
assert.match(finalNav,/v148-context-nav-active/);
assert.match(navCss,/\.v148-context-nav-active #v141DungeonNav\{display:flex !important;\}/);
assert.match(navCss,/\.v148-context-nav-active #bottomNav\{display:none !important;\}/);

assert.match(vfx,/className="v143-vfx-frame"/);
assert.match(vfx,/frame\.style\.backgroundImage=imageValue/);
assert.match(vfxCss,/\.v143-skill-stage\{[\s\S]*?overflow:visible;[\s\S]*?contain:layout style;/);
assert.match(vfxCss,/\.v143-vfx-sprite\{[\s\S]*?overflow:visible;[\s\S]*?background-image:none !important;/);
assert.match(vfxCss,/\.v143-vfx-frame\{[\s\S]*?overflow:hidden;/);
assert.match(vfxCss,/v143-vfx-sprite-active > \.v143-vfx-frame[\s\S]*?v143RasterCastFrames/);

assert.match(battle,/battle-player\.v174-cardless-unit>\.v174-battle-art\{[\s\S]*?inset:-49px -33px 30px!important;[\s\S]*?background-size:contain!important/);
assert.match(battle,/battle-monster\.v174-cardless-unit>\.battle-monster-name\{[\s\S]*?top:calc\(100% \+ 3px\)!important/);
assert.match(battle,/battle-monster\.v174-cardless-unit>\.v174-battle-art\{[\s\S]*?background-size:contain!important/);
assert.doesNotMatch(battle,/battle-monster\.v174-cardless-unit>\.v174-battle-art\{[\s\S]*?background-size:cover!important/);
assert.match(vfxCss,/battleMonsterArea \.v131-monster-row\{[\s\S]*?padding-bottom:18px/);
assert.match(enemy,/setProperty\("font-size","9px","important"\)/);
assert.match(bossCss,/gameplay-boss-card\[data-rank="boss"\][\s\S]*?overflow:visible;/);
console.log("shared nav / unclipped VFX / battle presentation regression guard passed");
''',encoding='utf-8')

          # Record the architectural handoff once.
          handoff=Path('HANDOFF.md')
          text=handoff.read_text(encoding='utf-8')
          heading='## 2026-09-14 — Shared Gameplay/Dungeon nav + unclipped VFX/battle art\n'
          if heading not in text:
              text+='\n\n'+heading+'''- Base: `dev@bba7cb1f1f896e8eb19f8cbcb7503bddc3dde5e4`; main untouched.
- `js/42-v148-combat-dungeon-fixes.js` is the sole markup renderer for the shared five-button Gameplay/Dungeon context nav. V141/V146 only delegate to the final owner.
- Formal cast VFX keep V142 timing + V143 renderer ownership: the fixed positioning node/stage may overflow, while `.v143-vfx-frame` alone clips a Sprite Sheet cell.
- V174 cardless presentation now renders player battle art at 1.5x its previous visual box; monster/BOSS art uses `contain`, names sit below resource bars, and enemy resource typography matches the player's thin HUD lane.
- Gameplay BOSS 9:16 cards permit visible overflow; source images remain unchanged. If a source bitmap is already cropped, CSS cannot restore missing pixels.
'''
              handoff.write_text(text,encoding='utf-8')
          PY
      - name: Focused regression tests
        run: |
          node tests/shared-nav-vfx-battle-layout-20260914.test.js
          node tests/v148-combat-dungeon-fixes.test.js
          node tests/v143-combat-dungeon-polish.test.js
          node tests/v153-fire-vfx.test.js
          node tests/boss-mobile-portrait-browser.test.js
      - name: Full test suite
        run: npm test
      - name: Rebuild production bundles
        run: npm run build
      - name: Validate built tree
        run: |
          npm run build:check
          git diff --check
      - name: Commit repaired owners and remove temporary workflow
        run: |
          rm -f .github/workflows/temp-shared-nav-vfx-battle-layout.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: unify context nav and unclamp battle presentation"
          git push origin HEAD:fix/shared-nav-vfx-battle-layout-20260914
